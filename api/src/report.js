module.exports = function( data ) {

	//This function is called every time anything is logged by winston.
	//The data being logged is passed to the function. Could be used to 
	//do some analysis of particular events to generate some kind of 
	//useage reports. 
}